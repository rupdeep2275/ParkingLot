package repositories;

import models.Gate;

import java.util.Optional;

public class GateRepository {
    public Optional<Gate> fetchGateById(Long gateId){
        //hit the db to get the date
        return Optional.empty();
    }
}
